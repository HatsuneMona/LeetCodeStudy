func maxProfit(prices []int) int {
	max := 0
	for i := 0; i < len(prices); i++ {
		buyPrices := prices[i]
		for j := 1; j < len(prices[i:]); j++ {
			if profit := prices[i+j] - buyPrices; profit > max {
				max = profit
			}
		}
	}
	return max
}
//runtime:340 ms
//memory:3.1 MB
